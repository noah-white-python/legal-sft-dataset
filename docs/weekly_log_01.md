# 第 1 周周报(weekly_log_01)

> Day 7 下午写。这份周报就是你的"面试弹药库",踩坑与解决部分尤其重要,每条都是一个故事。
> 下面带 ✏️ 的是待你填的,其余是已完成事项的记录。

## 一、最终数据

- 现成数据集(冷启动):`DISC-Law-SFT`,取 5000 条(零缺失,去重后 4994)
- 自建爬虫:✏️ 爬到多少条?来自哪些源?
- 合计有效问答:✏️ ___ 条

## 二、错误与稳定性

- 整体失败率:✏️ ___%(看 sqlite `dead_letters` 表条数 / 总尝试数)
- 失败主因前三:✏️ 1. ___ 2. ___ 3. ___

## 三、耗时

- 冷启动下载:几分钟
- 爬虫:✏️ 爬 ___ 条花了 ___(用这个估算上量目标是否现实)

## 四、踩坑与解决(至少 3 条,每条写清"怎么发现 → 怎么解决")

1. **Windows 控制台 GBK 编码**:打印 ✓/中文符号时 `UnicodeEncodeError` 直接崩。
   解决:脚本开头 `sys.stdout.reconfigure(encoding="utf-8")`,符号尽量用 ASCII。
2. **HuggingFace 国内不通**:`load_dataset` 卡死/超时。
   解决:`import datasets` 之前设 `HF_ENDPOINT=https://hf-mirror.com`,并用 streaming 流式取数,绕过整库生成报错。
3. ✏️ (爬虫阶段的坑:编码乱码 / 结构不一致 / 被限频…怎么发现、怎么解决)
4. ✏️ (可继续补)

## 五、还没搞定的(诚实记录,下周补)

- 异步:学到哪了?`src/async_demo.py` 跑通了吗?Semaphore 理解了吗?✏️
- Playwright:碰了没?✏️(本周非必需)

## 六、下周计划

数据清洗 → 去重(精确 + 近似 MinHash/SimHash)→ 质量过滤 → 格式化成 SFT 训练格式
(`{"instruction":..., "input":..., "output":...}`)。

## 七、一句话定位(看完综述后填)

我做的这个法律 SFT 数据集,处于 LLM 流程的「✏️ 预训练 → **SFT** → 对齐」哪一环、解决什么问题:
✏️ ___
